[project]
name = "nep-auto"
version = "0.2.0"
description = "基于 MaxVol 算法的 NEP 势函数主动学习框架"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ase>=3.26.0",
    "numpy>=2.4.0",
    "pynep",
    "pyyaml>=6.0.3",
    "scipy>=1.11.0",
    "tqdm>=4.67.1",
]

[project.scripts]
nep-auto-init = "nep_auto.initialize:main"
nep-auto-main = "nep_auto.main:main"
nep-auto-config = "nep_auto.config:main"
nep-auto-first-train = "nep_auto.first_train:main"
nep-auto-init-config = "nep_auto.init_config:main"

[tool.uv]
package = true

[[tool.uv.index]]
url = "https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple"
default = true

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["nep_auto*"]
exclude = ["test*", "nep_maker*"]

[tool.setuptools.package-data]
nep_auto = ["*.yaml"]

[tool.uv.sources]
pynep = { git = "https://github.com/bigd4/PyNEP.git" }

